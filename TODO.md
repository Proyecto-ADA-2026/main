# TODO - Ajustes para cumplir enunciado

- [ ] 1) Actualizar `interfaz_grafica.py`: validar `n` solo con `n >= 4` y agregar advertencia para `n` grande.
- [ ] 2) `interfaz_grafica.py`: construir árbol equilibrado para `A³` (agregar `self.arbol_A3` y construir en `generar()`).
- [ ] 3) `interfaz_grafica.py`: ampliar `buscar()` para medir también en `A³` y en `arbol_A3`.
- [ ] 4) `interfaz_grafica.py`: optimizar `multiplicar_matrices` usando transpuesta.
- [ ] 5) `interfaz_grafica.py`: implementar vista previa en UI para matrices grandes y guardar matrices en `.txt`.
- [ ] 6) (Opcional) Revisar y alinear `Proyecto_final.py` con los cambios de `interfaz_grafica.py`.
- [ ] 7) Probar ejecución de la GUI con `n=4`, `n=12` y `n` grande.

